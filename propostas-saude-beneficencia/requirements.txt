Flask==3.0.0
Werkzeug==3.0.1
gunicorn==21.2.0
requests==2.31.0
# Versao EXATA de proposito: o preenchimento dos PDFs depende do formato do fluxo de
# aparencia do pypdf e de PdfWriter._add_apstream_object (API interna da biblioteca).
# Subir de versao sem testar quebra o layout do documento que vai para a operadora.
pypdf==6.11.0
# Foto de documento -> pagina A4 no contrato (montagem.py) e reducao antes de mandar
# para a IA (leitor.py).
Pillow==11.3.0
# Renderiza pagina de PDF como imagem, para a IA conseguir ler documento em PDF.
pypdfium2==5.10.1
# Leitura automatica dos documentos. So e usada quando ANTHROPIC_API_KEY existe -
# sem a chave o app sobe igual e cai no caminho manual (prompt para o ChatGPT).
anthropic==0.116.0
# Carta de cancelamento do contrato anterior (venda administrativa). E o unico
# documento que o sistema cria do zero, em vez de preencher formulario da operadora.
reportlab==5.0.0
